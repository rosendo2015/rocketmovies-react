import styled from "styled-components";

export const Container = styled.div`
  color: ${({ theme }) => theme.COLORS.PINK};
  font-size: 24px;
  line-height: 32px;
  font-weight: 700;
  min-width: 265px;

`;
